<UserControl x:Class="GraphBuilder.Shell.Views.ContextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
             
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding NodesCount, StringFormat= Узлов: {0}}" Margin="10"/>
            <TextBlock Text="{Binding LinksCount, StringFormat= Связей: {0}}" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
